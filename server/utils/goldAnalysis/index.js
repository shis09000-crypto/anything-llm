const { FACTOR_REGISTRY, FACTOR_REGISTRY_SHA256 } = require("./contracts");
const { GoldAnalysisRuntime, goldAnalysisRuntime } = require("./runtime");

module.exports = {
  FACTOR_REGISTRY,
  FACTOR_REGISTRY_SHA256,
  GoldAnalysisRuntime,
  goldAnalysisRuntime,
};
